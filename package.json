{
  "name": "ai-consulting-landing",
  "version": "1.0.0",
  "description": "AI Consulting & Workshops Landing Page",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch",
    "build": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}